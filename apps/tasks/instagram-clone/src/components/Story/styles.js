import { StyleSheet } from "react-native";

export default StyleSheet.create({
  image: {
    width:60,
    height: 60,
    borderRadius: 30
  },
  storyBorder: {
    width: 65,
    height: 65,
    borderRadius: 30,
    alignItems: 'center',
    justifyContent: 'center'
  }
})